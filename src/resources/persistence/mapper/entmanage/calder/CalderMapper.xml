<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.skshieldus.esecurity.repository.entmanage.calder.CalderRepository">

	<select id="selectVisitNacIfView" parameterType="String" resultType="HashMap">
		/* CalderRepository.selectVisitNacIfView (dmVisit_NacIF_UView) */
		SELECT *
		FROM esecurity
		WHERE UPPER(USER_ID) = UPPER(#{ioEmpId})
	</select>
	
</mapper>